'use client'

import { Container } from '@mui/material';
import React from 'react';

const BasicLayoutPage=()=>{
  return (
    <Container maxWidth={false} style={{ backgroundColor: 'red', height: '100%', width: '100%' }}>
      {/* <h1>Hollo world</h1> */}
    </Container>
  );
}

export default BasicLayoutPage;